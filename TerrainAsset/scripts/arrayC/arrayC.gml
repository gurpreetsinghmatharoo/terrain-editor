/// @description  arrayC(num, color, tex);
/// @function  arrayC
/// @param num
/// @param  color
/// @param  tex
//Creates an array
var arr;

for(var i=0; i<argument_count; i++){
    arr[i] = argument[i];
}

return arr;
